# Project Rules

## GitHub Issues and PRs

This is a fork of `minio/minio`. All GitHub issues, pull requests, and comments
must target `rophy/minio` — never the upstream `minio/minio` repository.

When using `gh` CLI, always pass `--repo rophy/minio` explicitly.
